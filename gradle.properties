# Enable AndroidX support (Required for modern Android UI libraries)
android.useAndroidX=true

# Automatically convert legacy third-party dependencies to AndroidX
android.enableJetifier=true

# JVM Memory settings for Gradle build runner
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
